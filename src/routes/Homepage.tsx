import React from 'react';

export default () => (
    <div className="homepage-body">
        <p>Please use the "/market browser" command to get your unique ID</p>
        <input className="unique-id-input" type="text" name="unique-id-input" />
        <button type="button">Sign in</button>
    </div>
)